## To begin with need to install mysql server as follows:

* sudo apt update && apt install -y mysql-server

## Then to start mysql service:

* sudo service mysql start

## Finally to connect the mysql client:

* sudo mysql